version: '3.8'

services:
  api:
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - GIN_MODE=release
    volumes:
      - ./data:/app/data
    restart: unless-stopped
